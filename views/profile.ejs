<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= profile.name %></title>
    <style>
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif;
            background-image: url('<%= template.background %>');
            background-size: cover;
            background-position: center;
            color: <%= template.fontColor || '#333' %>;
            margin: 0;
            padding: 20px;
            display: flex;
            justify-content: center;
        }
        .profile-container {
            max-width: 400px;
            width: 100%;
            text-align: center;
            background: rgba(0, 0, 0, 0.4);
            padding: 20px;
            border-radius: 15px;
        }
        .profile-header {
            margin-bottom: 30px;
        }
        .profile-header h1 {
            font-size: 24px;
            margin: 10px 0;
        }
        .profile-header p {
            font-size: 16px;
        }
        .horoscope {
            background: rgba(255, 255, 255, 0.2);
            padding: 10px;
            border-radius: 8px;
            margin-top: 15px;
            font-style: italic;
        }
        .profile-links a {
            display: block;
            background-color: <%= template.buttonBgColor || profile.theme %>;
            color: <%= template.buttonFontColor || 'white' %>;
            padding: 15px;
            margin-bottom: 15px;
            border-radius: 8px;
            text-decoration: none;
            font-weight: 600;
            transition: transform 0.2s;
        }
        .profile-links a:hover {
            transform: scale(1.05);
        }
        .download-button-container {
            margin-top: 20px;
        }
        .download-button {
            display: inline-block;
            padding: 10px 20px;
            background-color: #2ecc71;
            color: white;
            text-decoration: none;
            border-radius: 5px;
            transition: background-color 0.3s;
        }
        .download-button:hover {
            background-color: #27ae60;
        }
    </style>
</head>
<body>
    <div class="profile-container">
        <header class="profile-header">
            <% if (profile.profilePicture) { %>
                <img src="/uploads/<%= profile.profilePicture %>" alt="Profile Picture" style="width:100px; height:100px; border-radius:50%;">
            <% } %>
            <h1><%= user.displayName %></h1>
            <p><%= profile.description %></p>
            <% if (locals.dailyHoroscope) { %>
                <div class="horoscope">
                    <strong>Daily Horoscope:</strong> <%= dailyHoroscope %>
                </div>
            <% } %>
        </header>
        <main class="profile-links">
            <% profile.links.forEach(link => { %>
                <a href="<%= link.url %>" target="_blank"><%= link.title %></a>
            <% }) %>
        </main>
        <div class="download-button-container">
            <a href="<%= new URL(req.originalUrl, 'http://' + req.headers.host).pathname %>/download" class="download-button">Download Profile</a>
        </div>
    </div>
    <footer>
        <p>Owner & Developer : Vzoel Fox's</p>
        <p>Instagram : @vzoel.fox_s</p>
    </footer>
</body>
</html>
